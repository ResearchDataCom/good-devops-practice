selector_to_html = {"a[href=\"#merging-a-feature-branch\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Merging a Feature Branch<a class=\"headerlink\" href=\"#merging-a-feature-branch\" title=\"Link to this heading\">#</a></h2><p>Because Git supports work occurring simultaneously in multiple\nbranches of a repository, the starting commit of a feature branch may\neventually no longer be the head of the main branch.  Merging a\nfeature branch without accounting for recent changes to the main\nbranch would effectively delete delivered code or approved changes\nfrom the Git repository.  By default, Git will handle this situation\nby creating a merge commit and prompting the user to fix any conflicts\n(when the same file was modified in both branches).</p><p>Instead, rebase the feature branch on the head of the main branch\nprior to merging to preserve the linear commit history of the Git\nrepository:</p>", "a[href=\"#feature-branch-workflow\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Feature Branch Workflow<a class=\"headerlink\" href=\"#feature-branch-workflow\" title=\"Link to this heading\">#</a></h1><p>Encapsulate development work in dedicated, temporary branches of a Git\nrepository.  The main branch of the repository only contains\nknown-good source code or configuration files, and the head of the\nmain branch always matches the latest release or live deployment.</p>", "a[href=\"#the-main-branch\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">The Main Branch<a class=\"headerlink\" href=\"#the-main-branch\" title=\"Link to this heading\">#</a></h2><p>The main branch does not contain work in progress except during\ninitial development, prior to the project\u2019s initial release or\ndeployment.</p>", "a[href=\"#origin\"]": "<aside class=\"footnote brackets\" id=\"origin\" role=\"doc-footnote\">\n<span class=\"label\"><span class=\"fn-bracket\">[</span><a href=\"#id1\" role=\"doc-backlink\">1</a><span class=\"fn-bracket\">]</span></span>\n<p>By default, clones of the authoritative repository call it\nthe <code class=\"docutils literal notranslate\"><span class=\"pre\">origin</span></code>\n<a class=\"reference external\" href=\"https://git-scm.com/book/en/v2/Git-Basics-Working-with-Remotes\">remote repository</a>.</p>\n</aside>", "a[href=\"../build/index.html\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Continuous Integration<a class=\"headerlink\" href=\"#continuous-integration\" title=\"Link to this heading\">#</a></h1>", "a[href=\"#the-head-of-the-main-branch\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">The Head of the Main Branch<a class=\"headerlink\" href=\"#the-head-of-the-main-branch\" title=\"Link to this heading\">#</a></h2><p>For infrastructure-as-code projects, staff should be able to redeploy\na service from scratch given the head of the main branch of the Git\nrepository, the service\u2019s secrets and other runtime configuration\nparameters, and a recent copy of the service\u2019s persistent data\nstorage.</p>", "a[href=\"#linear-commit-history\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Linear Commit History<a class=\"headerlink\" href=\"#linear-commit-history\" title=\"Link to this heading\">#</a></h2><p>Always use the <code class=\"docutils literal notranslate\"><span class=\"pre\">--ff-only</span></code> option to the <code class=\"docutils literal notranslate\"><span class=\"pre\">git</span> <span class=\"pre\">merge</span></code> and <code class=\"docutils literal notranslate\"><span class=\"pre\">git</span> <span class=\"pre\">fetch</span></code>\ncommands.  Likewise, always use <code class=\"docutils literal notranslate\"><span class=\"pre\">--rebase</span></code> option to the <code class=\"docutils literal notranslate\"><span class=\"pre\">git</span> <span class=\"pre\">pull</span></code>\ncommand, or set <code class=\"docutils literal notranslate\"><span class=\"pre\">pull.rebase</span></code> to <code class=\"docutils literal notranslate\"><span class=\"pre\">true</span></code> in the Git configuration.</p>", "a[href=\"#updating-a-feature-branch\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Updating a Feature Branch<a class=\"headerlink\" href=\"#updating-a-feature-branch\" title=\"Link to this heading\">#</a></h2><p>Because Git supports work occurring simultaneously in the same branch\nof multiple clones of a repository, the original head of a feature\nbranch can eventually no longer be the head of the corresponding\nremote-tracking branch.  Pushing changes commited to the feature\nbranch without accounting for recent changes to the remote-tracking\nbranch would effectively delete other developers\u2019 work from the Git\nrepository.  By default, Git will handle this situation by creating a\nmerge commit and prompting the user to fix any conflicts (when the\nsame file was modified in both branches).</p><p>Instead, rebase the feature branch on the head of the remote-tracking\nbranch prior to merging to preserve the linear commit history of the\nGit repository:</p>", "a[href=\"#moving-work-in-progress-to-a-feature-branch\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Moving Work in Progress to a Feature Branch<a class=\"headerlink\" href=\"#moving-work-in-progress-to-a-feature-branch\" title=\"Link to this heading\">#</a></h2><p>If instead the work in progress was mistakenly committed to the main\nbranch but <em>not yet</em> pushed to the authoritative repository, move the\nwork in progress to a feature branch:</p>", "a[href=\"#feature-branches\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Feature Branches<a class=\"headerlink\" href=\"#feature-branches\" title=\"Link to this heading\">#</a></h2><p>Limit a feature branch to a single <a class=\"reference external\" href=\"https://en.wikipedia.org/wiki/deliverable#\">deliverable</a>.\nThe branch name succinctly describes the work, e.g., <code class=\"docutils literal notranslate\"><span class=\"pre\">stepup-mfa</span></code>,\n<code class=\"docutils literal notranslate\"><span class=\"pre\">mfa-enforcement-fixes</span></code>.  Git supports multiple feature branches being\nworked simultaneously.  To reduce the likelihood of a merge conflict\nsynchronize the main branch with the authoritative repository<a class=\"footnote-reference brackets\" href=\"#origin\" id=\"id1\" role=\"doc-noteref\"><span class=\"fn-bracket\">[</span>1<span class=\"fn-bracket\">]</span></a>\nbefore creating a new feature branch, and configure\n<a class=\"reference external\" href=\"https://git-scm.com/book/en/v2/Git-Branching-Remote-Branches\">remote tracking</a>\nimmediately after:</p>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(` ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false,

            });
        };
    };
    console.log("tippy tips loaded!");
};
